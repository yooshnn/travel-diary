<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.td.traveldiary.domain.content.repository.ContentTypeRepository">

    <select id="findAll" resultType="com.td.traveldiary.domain.content.entity.ContentType">
        SELECT id, name
        FROM content_type
        ORDER BY id
    </select>

</mapper>